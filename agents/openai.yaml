interface:
  display_name: "Humanizer — Persian Writing"
  short_description: "Native Persian editing with meaning and voice preserved"
  default_prompt: "Use $humanizer to improve this Persian text only where justified; preserve meaning, register, uncertainty, terminology, and writer voice."

policy:
  allow_implicit_invocation: true
