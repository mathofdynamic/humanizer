interface:
  display_name: "Humanizer — Persian Writing"
  short_description: "Natural Persian editing with voice preserved"
  default_prompt: "Use $humanizer to rewrite this Persian text naturally while preserving its meaning and register."

policy:
  allow_implicit_invocation: true
